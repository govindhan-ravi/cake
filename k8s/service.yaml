apiVersion: v1
kind: Service
metadata:
  name: cake-site-service
  labels:
    app: cake-site
spec:
  type: LoadBalancer
  selector:
    app: cake-site
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 80
